<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VinylsMobile"
        tools:targetApi="31">
        <activity
            android:name=".view.PerformerDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_performer_detail"
            android:theme="@style/Theme.VinylsMobile" />

        <service
            android:name=".service.CollectorService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".service.Collector"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".view.AlbumDetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.CollectionActivity"
            android:exported="false"
            android:label="@string/title_activity_collection"
            android:theme="@style/Theme.VinylsMobile" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>